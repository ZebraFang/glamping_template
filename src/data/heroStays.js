/**
 * Stay catalogue for hero booking UI (bar + mobile sheet + desktop panel).
 * Re-export keeps existing import paths stable; full records live in staysCatalog.js.
 */
export { HERO_STAYS, STAYS_CATALOG } from './staysCatalog.js'
