/**
 * Shared layout breakpoints — keep in sync with Nav / Hero booking UI.
 * @see Nav.module.css (.desktopNav, overlay)
 * @see HeroBookingBar.module.css
 */
export const NAV_DESKTOP_MIN_PX = 900

export const desktopNavMediaQuery = `(min-width: ${NAV_DESKTOP_MIN_PX}px)`
